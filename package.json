{
  "name": "ladies-glam",
  "version": "1.0.0",
  "scripts": {
    "start": "cd server && npm start",
    "build": "cd admin && npm run build",
    "install-server": "cd server && npm install",
    "install-admin": "cd admin && npm install",
    "heroku-postbuild": "npm run install-server && npm run install-admin && npm run build"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}